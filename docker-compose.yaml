version: '3'

services:
  django:
    build: 
      context: .
      target: django-app
    restart: always
    env_file: .env
    ports:
      - 8000:8000
    volumes:
      - media:/usr/src/app/media
      - static:/usr/src/app/staticfiles

volumes:
  media:
  static:
      